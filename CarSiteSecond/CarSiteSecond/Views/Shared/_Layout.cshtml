<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/Site.css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="flex-container border-mine">
                @Html.ActionLink("Home", "Index", "Home")
                @Html.ActionLink("New Inventory", "About", "Home")
                @Html.ActionLink("Used Inventory", "Contact", "Home")
                @Html.ActionLink("New Inventory", "Contact", "Home")
                @Html.ActionLink("Specials", "Contact", "Home")
                @Html.ActionLink("Contact", "Contact", "Home")
                @Html.ActionLink("Sales", "Contact", "Home")
                @Html.ActionLink("Admin", "Contact", "Home")
            </div>
            </div>
        </div>

    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - My ASP.NET Application</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
